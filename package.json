{
  "name": "nautilus-tech-site",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio estático de Nautilus Tech (HTML + Tailwind + JS)",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./css/tailwind.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./css/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  }
}
